{
  "identifier": "default",
  "description": "Default capabilities for PyWR Canvas",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
